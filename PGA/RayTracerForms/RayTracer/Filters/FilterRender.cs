using PGA.Filters;

namespace PGA
{
    class FilterRender
    {
        //public static  Color ImageProcess(int x,int y , Color[,] image, IFilter filter)
        //{
        //    double red = 0.0, green = 0.0, blue = 0.0;
        //    int count = 0;
        //    for (int filterY = 0; filterY <= filter.Heigth; filterY++)
        //    {
        //        for (int filterX = 0; filterX <= filter.Width; filterX++)
        //        {
        //            int imageX = x + (filterX - 1);
        //            int imageY = y + (filterY - 1);
        //            red += image[imageX, imageY].Red * filter.Matrix[filterY, filterX];
        //            green += image[imageX, imageY].Green * filter.Matrix[filterY, filterX];
        //            blue += image[imageX, imageY].Blue * filter.Matrix[filterY, filterX];
        //            count++;
        //        }
        //    }

        //    return new Color(red,green,blue);
        //}
    }
}
